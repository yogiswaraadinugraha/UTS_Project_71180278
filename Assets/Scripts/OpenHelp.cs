using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OpenHelp : MonoBehaviour
{
    public Canvas help;
    // Start is called before the first frame update
    public void OpenViewHelp()
    {
        help.enabled = true;
    }
}
